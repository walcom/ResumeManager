@model IEnumerable<ResumeManager.Models.SaleInvoice>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SalesInvoiceCode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SalesInvoiceDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Customer.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Remarks)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.VatAmount)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SalesInvoiceCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SalesInvoiceDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Customer.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Remarks)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.VatAmount)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SalesInvoiceID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SalesInvoiceID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SalesInvoiceID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
